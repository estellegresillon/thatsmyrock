let wrapper = document.getElementById('<%= "icons_#{@album.id}" %>');
wrapper.querySelectorAll('a').forEach((icon) => {
  if ('collected' == '<%= @status %>') {
    wrapper.remove();
  } else if (icon.dataset.status == '<%= @status %>') {
    console.log(icon)
    icon.remove();
  };
})
